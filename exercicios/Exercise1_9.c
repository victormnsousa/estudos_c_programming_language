#include <stdio.h>

//Exercicio de eliminador de espaços 

int main(void)
{
	char c[1000];
	char vazio = (' ');
	
	while ((c = getchar()) != EOF)
		++vazio;
		if(vazio =< '  ' || vazio ' ')
		{
			printf ("%s/n, c, vazio);
		}
}
		
