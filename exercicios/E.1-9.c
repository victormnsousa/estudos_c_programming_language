#include <stdio.h>

int main(void)
{
	int a, b, blanks, tabs, newlines;
	
	a = 0;
	while ((b = getchar()) != EOF)
		if (blanks == ' ')
			if(tabs == '\t')
				if(newlines == '\n')
						++a;
		printf("Vazios: %d, blanks, Parágrafos: %d, tabs, Linhas: %d, newlines");
		return 0
} 
	
	
