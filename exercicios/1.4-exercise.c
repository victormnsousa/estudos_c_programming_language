#include <stdio.h>

#define LOWER 0
#define UPPER 50
#define STEP 5

int main(void)
{
	int celsius;
	
	for (celsius = UPPER; celsius <= UPPER; celsius = celsius + STEP)
		
	printf("%3d\t &6.1f\t\n*", celsius, (5.0/9.0)*(celsius+32));
}
